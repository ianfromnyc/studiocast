This directory contains *metadata only* for a StudioCast Open Audio model pack.

No model binaries are shipped in the StudioCast repo.

Use:
  ./scripts/install.sh open-audio-models

Upstream model:
  FastEnhancer (MIT) — https://github.com/aask1357/fastenhancer
